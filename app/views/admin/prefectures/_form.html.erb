<%= form_for([:admin, @prefecture]) do |f| %>
  <% if @prefecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prefecture.errors.count, "error") %> prohibited this prefecture from being saved:</h2>

      <ul>
      <% @prefecture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prefecture_name %><br />
    <%= f.text_field :prefecture_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
