<%= form_for(@admin_season_team) do |f| %>
  <% if @admin_season_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_season_team.errors.count, "error") %> prohibited this admin_season_team from being saved:</h2>

      <ul>
      <% @admin_season_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :season %><br />
    <%= f.text_field :season %>
  </div>
  <div class="field">
    <%= f.label :division %><br />
    <%= f.text_field :division %>
  </div>
  <div class="field">
    <%= f.label :team %><br />
    <%= f.text_field :team %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
