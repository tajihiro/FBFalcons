<%= form_for([:admin, @season_roster]) do |f| %>
  <% if @season_roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season_roster.errors.count, "error") %> prohibited this season_roster from being saved:</h2>

      <ul>
      <% @season_roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :season %><br />
    <%= f.text_field :season %>
  </div>
  <div class="field">
    <%= f.label :division %><br />
    <%= f.text_field :division %>
  </div>
  <div class="field">
    <%= f.label :member %><br />
    <%= f.text_field :member %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
